name: Example GitHub Action

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      
      - name: 22f3002842@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Finish
        run: echo "Action Completed"
